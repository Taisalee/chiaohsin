---
import Layout from "../layouts/Layout.astro";
import PageHero from "../components/PageHero.astro";
import { getCollection } from "astro:content";
import { Image } from "astro:assets";
import company from "../data/company.json";

const allPosts = await getCollection("blog", ({ data }) => data.draft !== true);

const sortedPosts = allPosts.sort(
    (a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf(),
);

// SEO 設定
const pageTitle = "如何挑選手套 - 教學文章";
const pageDescription = `${company.name} 專業教學文章，教您如何選擇最適合的工作手套與防護用品。`;
---

<Layout
    title={pageTitle}
    description={pageDescription}
    keywords={["手套選購", "工作手套", "防護用品", "教學文章"]}
    canonical="/blog"
>
    <PageHero title="如何挑選工作手套｜棉紗・膠手套・尼龍手套選購指南" description="喬新針織專業教學文章：棉紗手套重量選法、膠手套材質比較、尼龍手套應用場合，幫助您找到最適合的防護用品">
      <nav slot="breadcrumb" class="ph-breadcrumb" aria-label="breadcrumb">
        <a href="/">首頁</a>
        <span class="ph-breadcrumb-sep" aria-hidden="true">／</span>
        <span class="ph-breadcrumb-cur">如何挑選手套</span>
      </nav>
    </PageHero>

    <!-- Articles Grid -->
    <section class="py-16 px-4 bg-base-100">
        <div class="max-w-6xl mx-auto">
            {
                sortedPosts.length > 0 ? (
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                        {sortedPosts.map((post) => (
                            <a href={`/blog/${post.id.replace(/\/index$/, '')}`} class="group">
                                <article class="card bg-base-100 shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2 h-full border border-base-200 overflow-hidden">
                                    <figure class="aspect-video overflow-hidden">
                                        <Image
                                            src={post.data.image}
                                            alt={post.data.title}
                                            class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
                                            width={600}
                                            height={337}
                                        />
                                    </figure>
                                    <div class="card-body">
                                        {post.data.category && (
                                            <div class="badge badge-primary badge-outline mb-2">
                                                {post.data.category}
                                            </div>
                                        )}
                                        <h2 class="card-title text-xl group-hover:text-primary transition-colors">
                                            {post.data.title}
                                        </h2>
                                        <p class="text-gray-500 line-clamp-3">
                                            {post.data.description}
                                        </p>
                                        <div class="flex items-center justify-between mt-4 pt-4 border-t border-base-200">
                                            <time
                                                class="text-sm text-gray-400"
                                                datetime={post.data.publishDate.toISOString()}
                                            >
                                                {post.data.publishDate.toLocaleDateString(
                                                    "zh-TW",
                                                    {
                                                        year: "numeric",
                                                        month: "long",
                                                        day: "numeric",
                                                    },
                                                )}
                                            </time>
                                            <span class="text-primary group-hover:translate-x-1 transition-transform">
                                                閱讀更多 →
                                            </span>
                                        </div>
                                    </div>
                                </article>
                            </a>
                        ))}
                    </div>
                ) : (
                    <div class="text-center py-16">
                        <div class="text-6xl mb-4">📝</div>
                        <h2 class="text-2xl font-bold mb-2">即將推出</h2>
                        <p class="text-gray-500">
                            我們正在準備精彩的教學內容，敬請期待！
                        </p>
                    </div>
                )
            }
        </div>
    </section>

    <!-- CTA Section -->
    <section class="py-16 px-4 bg-base-200">
        <div class="max-w-4xl mx-auto text-center">
            <h2 class="text-2xl md:text-3xl font-bold mb-4">有任何問題嗎？</h2>
            <p class="text-lg text-gray-600 mb-8">
                我們的專業團隊隨時為您提供手套選購建議
            </p>
            <a href="/about#contact" class="btn btn-primary btn-lg text-white">
                聯絡我們
            </a>
        </div>
    </section>
</Layout>
